// BACKEND
const BE_URL: string = "http://localhost"
const BE_PORT: string = "5050"
const BE_PATH: string = `${BE_URL}:${BE_PORT}/api`

// Customization
const LOGO_PATH: string = '/img/logo.png'

const MIN_LENGTH_NAME: number = 5
const MAX_LENGTH_NAME: number = 150

export {
  BE_PATH,
  LOGO_PATH,
  MIN_LENGTH_NAME,
  MAX_LENGTH_NAME
}